package com.app.redcarga.planning.domain.model.valueobjects;

public enum RouteShape {
    DD, // Departamento -> Departamento (provincias NULL)
    PP  // Provincia -> Provincia (provincias NOT NULL)
}
